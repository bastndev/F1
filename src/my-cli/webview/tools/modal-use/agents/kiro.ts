/**
 * Kiro usage adapter — command: /usage · policy: idle-only (must NOT be
 * injected while a task is running; doing so corrupts the input line).
 *
 * TODO: move parseKiroUsage here behind the UsageAgent interface.
 */
export {};
