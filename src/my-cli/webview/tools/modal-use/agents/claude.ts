/**
 * Claude usage adapter — command: /usage · policy: always (tolerates a
 * usage request in any session state).
 *
 * TODO: move parseClaudeUsage here behind the UsageAgent interface.
 */
export {};
