/**
 * Usage agent registry: resolves an active CLI label to its UsageAgent
 * adapter, falling back to unsupportedAgent for any CLI that doesn't
 * expose usage data.
 *
 * TODO: wire up claude / kiro / codex / antigravity / unsupported.
 */
export {};
