/**
 * Shared text helpers used by the per-agent parsers: ANSI stripping,
 * percent clamping/formatting, line cleaning.
 *
 * TODO: move stripAnsi / clampPercent / parsePercent / formatPercent /
 * getCleanLine / stripUsageVisualBar / titleCase here.
 */
export {};
