/**
 * "Smart + Skills" constants. Pure data — no vscode, no fs, no DOM.
 */

/** The phrase the CLI is asked to end its first reply with (its own readiness signal). */
export const SMART_READY_MESSAGE = 'I am ready for work ✅';
