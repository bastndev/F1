/**
 * Front door for the "Smart + Skills" feature — the host-side import surface.
 * The webview imports the overlay from ./webview/smart-skeleton directly.
 */

export { SmartService } from './core/smart-service';
