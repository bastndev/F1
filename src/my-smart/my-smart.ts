/**
 * Front door for the "Smart + Skills" feature — the only file outside
 * src/my-smart/ that other code (src/my-cli, src/extension.ts) should import.
 *
 * Scaffold only — no implementation yet.
 */
