/**
 * "Smart + Skills" orchestrator (host / Node).
 *
 * Runs the cheap prep when a CLI is launched with Smart mode ON (create .f1/,
 * copy the rules skill, ensure a basic project map) and the auto-cleanup of the
 * generated artifacts (.f1/, graphify-out/). Reuses my-memory via its front door.
 * Nothing is created on toggle — only on CLI launch.
 *
 * Scaffold only — no implementation yet.
 */
