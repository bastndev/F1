/**
 * Custom launch skeleton for "Smart + Skills" (webview / DOM) — distinct from
 * panel-terminal/boot-skeleton.ts. Shows while the host preps context, then
 * surfaces "I am ready for work ✅" in the chat.
 *
 * Scaffold only — no implementation yet.
 */
